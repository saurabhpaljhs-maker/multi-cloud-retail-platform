apiVersion: v1
kind: Service
metadata:
  name: retail-service
  labels:
    app: retail
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: retail
